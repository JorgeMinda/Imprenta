# Base de datos (Supabase o PostgreSQL local)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/contabilidad

# JWT Secret para autenticación
JWT_SECRET=cambia-este-valor-por-un-string-aleatorio-seguro

# URLs de la app (para desarrollo local)
WASP_SERVER_URL=http://localhost:3001
WASP_WEB_CLIENT_URL=http://localhost:3000
